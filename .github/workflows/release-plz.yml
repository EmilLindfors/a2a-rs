name: Release-plz

permissions:
  pull-requests: write
  contents: write

on:
  push:
    branches:
      - master
  workflow_dispatch:

env:
  # Force Node 20-pinned JavaScript actions onto Node 24 (silences the
  # deprecation annotation until the actions update).
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

# Standard release-plz flow with per-crate tags (see release-plz.toml):
#  - `release-plz-pr` keeps an open "release" PR with the pending version bumps
#    and changelog. Merging it lands the new versions on master.
#  - `release-plz-release` then publishes each crate whose version is ahead of
#    crates.io and pushes its `{package}-v{version}` tag.
jobs:
  release-plz-release:
    name: Release-plz release
    runs-on: ubuntu-latest
    # Publish ONLY when the push is the merged release PR, or on an explicit
    # manual dispatch. This is the release gate: ordinary pushes to master never
    # attempt a publish, so a half-finished workspace state or an accidental
    # hand-edited version bump on master can no longer auto-release. The actual
    # version bumps are owned by the release PR (release-plz-pr below).
    #
    # Both merge shapes are accepted because master is a protected branch, so
    # every release lands through the PR button and the commit subject depends on
    # which one was pressed: a squash merge reads "chore: release", a merge commit
    # reads "Merge pull request #N from OWNER/release-plz-<timestamp>". Matching
    # only the first silently skipped the publish on 2026-08-15 — the version
    # bumps landed on master, nothing reached crates.io, and release-plz then
    # re-proposed the same bump every push, because it reads the last *published*
    # version, not the one in Cargo.toml.
    if: >-
      github.event_name == 'workflow_dispatch' ||
      startsWith(github.event.head_commit.message, 'chore: release') ||
      (startsWith(github.event.head_commit.message, 'Merge pull request') &&
      contains(github.event.head_commit.message, '/release-plz-'))
    steps:
      - name: Checkout repository
        uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Cache dependencies
        uses: Swatinem/rust-cache@v2

      - name: Install cargo-semver-checks
        uses: taiki-e/install-action@v2
        with:
          tool: cargo-semver-checks

      - name: Run release-plz (release)
        uses: release-plz/action@v0.5
        with:
          command: release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          CARGO_REGISTRY_TOKEN: ${{ secrets.CARGO_REGISTRY_TOKEN }}

  release-plz-pr:
    name: Release-plz PR
    runs-on: ubuntu-latest
    concurrency:
      group: release-plz-${{ github.ref }}
      cancel-in-progress: false
    steps:
      - name: Checkout repository
        uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Cache dependencies
        uses: Swatinem/rust-cache@v2

      - name: Install cargo-semver-checks
        # Lets release-plz auto-detect breaking API changes and bump the minor
        # (0.x) without relying on `!`/`BREAKING CHANGE:` in commit messages.
        uses: taiki-e/install-action@v2
        with:
          tool: cargo-semver-checks

      - name: Run release-plz (release-pr)
        uses: release-plz/action@v0.5
        with:
          command: release-pr
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          CARGO_REGISTRY_TOKEN: ${{ secrets.CARGO_REGISTRY_TOKEN }}
