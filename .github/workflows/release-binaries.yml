name: Release Binaries

on:
  release:
    types: [published]

permissions:
  contents: write

jobs:
  upload-assets:
    name: Build and upload binaries
    strategy:
      matrix:
        include:
          - target: x86_64-unknown-linux-gnu
            os: ubuntu-latest
          - target: aarch64-unknown-linux-gnu
            os: ubuntu-latest
          - target: x86_64-apple-darwin
            os: macos-latest
          - target: aarch64-apple-darwin
            os: macos-latest
          - target: x86_64-pc-windows-msvc
            os: windows-latest
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      # Install cross for cross-compilation on Linux
      - name: Install cross
        if: runner.os == 'Linux'
        run: cargo install cross --git https://github.com/cross-rs/cross

      - name: Build binary
        shell: bash
        run: |
          if [ "${{ runner.os }}" = "Linux" ]; then
            cross build --release --bin a2a --target ${{ matrix.target }} --features "mcp-server,reimbursement-agent"
          else
            cargo build --release --bin a2a --target ${{ matrix.target }} --features "mcp-server,reimbursement-agent"
          fi

      - name: Package binary (Unix)
        if: runner.os != 'Windows'
        run: |
          tar -czvf a2a-${{ matrix.target }}.tar.gz -C target/${{ matrix.target }}/release a2a

      - name: Package binary (Windows)
        if: runner.os == 'Windows'
        run: |
          Compress-Archive -Path target/${{ matrix.target }}/release/a2a.exe -DestinationPath a2a-${{ matrix.target }}.zip

      - name: Upload Release Asset (Unix)
        if: runner.os != 'Windows'
        uses: softprops/action-gh-release@v2
        with:
          files: a2a-${{ matrix.target }}.tar.gz

      - name: Upload Release Asset (Windows)
        if: runner.os == 'Windows'
        uses: softprops/action-gh-release@v2
        with:
          files: a2a-${{ matrix.target }}.zip
