[profile.default]
# Fail fast after 3 test failures to save resources
max-fail = 3

# Add retries for flaky tests
retries = 2

# Slow timeout to prevent hanging tests from indefinitely using resources
slow-timeout = { period = "60s", terminate-after = 3 }
